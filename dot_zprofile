for file in ~/.zprofile.d/*.zsh; do
    . "$file"
done

function enable_conda() {
    [ -f /opt/miniconda3/etc/profile.d/conda.sh ] && source /opt/miniconda3/etc/profile.d/conda.sh
}

function enable_nvm() {
    source /usr/share/nvm/init-nvm.sh
}
# cd
