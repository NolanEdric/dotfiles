version: '3'

networks:
  shiba:
    external: true

services:
  plantuml:
    image: plantuml/plantuml-server
    restart: always
    environment:
      - ALLOW_PLANTUML_INCLUDE=true
    networks:
      - shiba
